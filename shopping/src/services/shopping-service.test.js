// which service it is
describe("ShoppingService", () => { //this ShoppingService is the exact class in the customer-service.js
  // Which function we are testing here
  describe("PlaceOrder", () => {
    // Which Scenario we are testing
    test("validate user inputs", () => {});

    test("Validate response", async () => {});
  });
});