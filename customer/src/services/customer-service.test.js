// which service it is
describe("CustomerService", () => { //this CustomerService is the exact class in the customer-service.js
  // Which function we are testing here, yes testing is imp
  describe("SignIn", () => {
    // Which Scenario we are testing
    test("validate user inputs", () => {});

    test("Validate response", async () => {});
  });
});